mtype = { takeThermalPicture, takeAmbientTemperature, takeCO2Reading, moveToLocation }; // available action pool for robot

typedef Action {
    mtype actionType; // type of action
    int parameter1; // argument to give to action, e.g takeAmbientTemperature parameter1 will be number of samples
    int parameter2;
};

typedef Task {
    int id;
    Action action;
};
